#For Master-ansible-VM
sudo apt update -y && sudo apt upgrade -y

#To generate public key for access of all the slave VM'slave
ssh-keygen                  #copy id_5356342.pub contant to all the Vm's path to sudo vim .ssh/authentication_key
sudo cat id_ed25519.pub

#To check the all slaveVM's is accessable or not use below ansible command
ansible all -m ping

#create ansible directory if not present 
sudo mkdir /etc/ansible

#create hosts file into this path /etc/ansible/hosts
sudo vim /etc/ansible/hosts

#create ansible configure file into this path /etc/ansible/ansible.cfg
sudo vim /etc/ansible/ansible.cfg

#Now, Create the ansible_playbooks directory
sudo mkdir ansible_playbooks

#In this ansible_playbooks directory, create playbooks
1. Jenkins_vm_setup.yml
2. Multi_vm_setup.yml
3. single_vm_setup.yml

#To run playbooks use below ansible commands
ansible-playbooks <playbooks_name.yml>